steps:
  # Шаг 1: Собираем Docker-образ по чистому пути
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build', 
      '-t', 'us-central1-docker.pkg.dev/ailbee/ailbee-enterprise/ailbee-enterprise-hub:$COMMIT_SHA', 
      '-f', 'backend/Dockerfile', 
      'backend'
    ]

  # Шаг 2: Деплоим с флагами отладки
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: [
      'run', 'deploy', 'ailbee-enterprise-hub',
      '--image', 'us-central1-docker.pkg.dev/ailbee/ailbee-enterprise/ailbee-enterprise-hub:$COMMIT_SHA',
      '--region', 'us-central1',
      '--project', 'ailbee',
      '--no-traffic',          # Не переключаем трафик, чтобы обойти зависание
      '--verbosity=debug'      # Включаем максимальную детализацию логов
    ]

options:
  logging: CLOUD_LOGGING_ONLY
